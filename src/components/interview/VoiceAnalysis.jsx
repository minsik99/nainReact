import React, { useEffect, useState } from "react";
import { observer } from "mobx-react";
import { useRouter } from 'next/router';

const VoiceAnalysis = observer(() => {

});

export default VoiceAnalysis;